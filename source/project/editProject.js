import {
    projectID,
    title,
    description,
    taskList,
    deadline, 
    priority,
    dateCreated,
    tasksCompleted
    } from '../backend/ProjectTable.js';

    let PROJECT_ID = "";

    window.addEventListener("DOMContentLoaded", init);